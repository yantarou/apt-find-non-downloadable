[project]
name = "apt-find-non-downloadable"
version = "0.4.0"
description = "Find installed packages that are not downloadable via configured APT sources."
readme = "README.md"
requires-python = ">=3.13"
dependencies = []
